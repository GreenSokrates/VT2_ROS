#include <http_server/HTTPScript_status.h>
#include <http_server/HTTPNode.h>

string HTTPScript_status::call(vector<string> names, vector<string> values)
{
    string response;
    /*
    response += " <h2> Montagestus ist: ";
    if (busy_)
    {
        response += "Busy = True";
    }
    else
    {
        response += "Busy = False";
    }
    if (idle_)
    {
        response += "Idle = True";
    }
    else
    {
        response += "Idle = False";
    }
    if (error_)
    {
        response += "ERROR = True";
    }
    else
    {
        response += "ERROR = False";
    }*/

    return response;
}
